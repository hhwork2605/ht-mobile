# Tiến độ: Đa ngôn ngữ VI/EN — Phase 3
slug: i18n
Cập nhật: 2026-06-17 04:20
Cổng hiện tại: 6

## Trạng thái cổng
- [x] 0. Thiết kế/UI      — DONE (tự dựng switcher theo header)
- [x] 1. Spec            — DONE (duyệt)
- [x] 2. Contract        — DONE (duyệt; không schema)
- [x] 3. Test (red)      — DONE (LanguageOptions đỏ)
- [x] 4. Implement       — DONE (build xanh, 115 test pass)
- [x] 5. Review subagent — DONE (Đạt, 0 blocker)
- [x] 6. Review tay       — DONE (duyệt M1 og:locale động + M2 cookie Secure)
- [x] 7. Test (green)     — DONE (115 test pass)
- [x] 8. Commit           — IN_PROGRESS

## Next action
- Smoke-test đổi VI/EN. XONG Phase 3 (cổng thanh toán đã hoãn).

## Vấn đề mở (reviewer Cổng 5)
- [M1] og:locale cứng vi_VN → động theo uiLang (vi_VN/en_US). [M2] cookie culture thêm Secure.
- Low: key Hotline chưa dùng (để dành); GET đổi cookie (chấp nhận cho đổi ngôn ngữ).
- Điểm tốt: UseRequestLocalization đúng vị trí; Resolve chặn culture lạ; returnUrl chống open-redirect;
  resx khớp khóa; Clean Architecture (LanguageOptions thuần, không đụng Domain/Application).

## Contract (Cổng 2) — chốt
- KHÔNG entity/DB/migration.
- Web infra:
  - LanguageOptions (thuần): Supported={vi,en}, Default=vi, IsSupported(code), Resolve(code)→supported|default (case-insensitive).
  - Resources/SharedResource.cs (marker) + Resources/SharedResource.resx (vi, neutral) + SharedResource.en.resx (en). Khóa semantic.
  - LanguageController GET /changelanguage(culture,returnUrl): set cookie CookieRequestCultureProvider.MakeCookieValue → redirect UrlSafety.SafeLocalUrl(returnUrl).
  - Program.cs: AddLocalization(ResourcesPath="Resources") + AddViewLocalization; UseRequestLocalization (vi default + en, cookie provider) trước routing.
  - _Layout: @inject IStringLocalizer<SharedResource> L; <html lang=culture>; LangSwitcher VI|EN (giữ returnUrl); localize chuỗi header/footer/topbar.
- Docs: api.md (/changelanguage); conventions §Đa ngôn ngữ đã có.

## Next action
- Chờ duyệt Contract → Cổng 3 test LanguageOptions (red) → Cổng 4.
- [ ] 2. Contract        — TODO
- [ ] 3. Test (red)      — TODO
- [ ] 4. Implement       — TODO
- [ ] 5. Review subagent — TODO
- [ ] 6. Review tay       — TODO
- [ ] 7. Test (green)    — TODO
- [ ] 8. Commit          — TODO

## Hiện trạng
- CHƯA có localization: Program.cs không AddLocalization/UseRequestLocalization; không Resources/; không LangSwitcher.
- Quy ước (conventions §Đa ngôn ngữ): chuỗi UI qua .resx + IStringLocalizer; nội dung DB qua entity Translation.

## Phạm vi dự kiến (chốt ở Cổng 1)
- IN: RequestLocalization vi (default) + en; cookie culture; /changelanguage; LangSwitcher ở header; .resx
  (SharedResource) + localize chuỗi UI chính (header/footer/nút chung).
- OUT (đề xuất): dịch nội dung DB (tên SP/mô tả qua Translation) — hoãn; hreflang giữ vi/x-default (cookie-based,
  không URL /en).

## Next action
- Chốt Cổng 0 (tự dựng switcher) → Cổng 1 Spec.
